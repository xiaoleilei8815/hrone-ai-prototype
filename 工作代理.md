# OTWS 工作代理需求清单

> 文档名称：工作代理  
> 适用角色：HR、文员、组长  
> 文档状态：最终版  
> 更新日期：2026-09-07

## 一、需求背景

当用户在一段时间内无法处理工作时，可以独立设置代理人，由代理人在指定时间内代为处理相关待办。本功能不包含请假提交、请假审批或与休假单联动的操作。

## 二、产品目标

- 用户可以设置工作代理人、有效时间及代理范围。
- 代理生效后，符合范围的新待办自动分配给代理人。
- 是否转移委托人的存量待办，由“代理已有待办”字段决定。
- 到达代理结束时间后，系统自动停用代理并转回未完成任务。
- 委托人和代理人均可识别、查看和处理代理任务。
- 代理关系、任务流转及实际处理人均可追溯。

## 三、角色与职责

| 角色 | 核心职责 |
|---|---|
| 委托人 | 设置代理人、代理范围、代理时间及是否代理已有待办；查看、修改、取消或提前结束代理 |
| 代理人 | 在代理有效期内接收并处理代理任务 |
| HR/管理员 | 查看全部代理关系，代为设置或结束代理，处理代理异常 |
| 系统 | 根据开始时间自动启用代理；根据范围和“代理已有待办”配置转派任务；根据结束时间自动停用代理、转回未完成任务、发送通知并记录日志 |

## 四、核心业务流程

1. 委托人进入“工作代理”页面并新增代理。
2. 设置代理人、开始时间、结束时间、“代理已有待办”和代理范围。
3. 系统校验用户有效性、时间和代理关系冲突。
4. 保存成功后，代理进入“待生效”；开始时间已到则立即启用。
5. 启用时，系统按照“代理已有待办”的配置决定是否转移存量待办。
6. 有效期内产生的新待办，根据代理范围自动分配给代理人。
7. 委托人和代理人均可处理任务，系统控制并发并阻止重复提交。
8. 到达结束时间后，系统自动停用代理，将未完成任务转回委托人。
9. 后续新待办恢复分配给委托人，系统通知相关人员并记录日志。

## 五、功能需求

### 5.1 设置入口

- 提供独立的“工作代理”入口。
- 用户可以为本人设置代理。
- HR或管理员可以在管理端代员工设置代理。
- 工作台展示当前有效或即将生效的代理关系。
- 支持查看“我设置的代理”和“我代理的工作”。

### 5.2 设置字段

| 字段 | 必填 | 默认值 | 配置说明 |
|---|---:|---|---|
| 委托人 | 是 | 当前用户 | 本人设置时自动带出；管理员设置时可选择员工 |
| 代理人 | 是 | 无 | 选择具体代理人 |
| 代理开始时间 | 是 | 无 | 精确到分钟 |
| 代理结束时间 | 是 | 无 | 精确到分钟，必须晚于开始时间 |
| 代理已有待办 | 是 | 是 | 可选“是”“否” |
| 代理范围 | 是 | 全部 | 可选“全部”“指定” |
| 指定范围 | 条件必填 | 无 | 代理范围为“指定”时显示，可选“需求指派”“用工需求审批” |
| 代理说明 | 否 | 无 | 补充说明 |

通知方式不作为设置字段，通知渠道按照系统统一配置执行。

### 5.3 “代理已有待办”规则

#### 选择“是”

- 代理生效时识别委托人在代理范围内的存量未处理待办。
- 将符合条件的存量待办转给代理人。
- 已完成、已取消、已关闭、已失效的任务不转移。
- 转移后的任务标记为“代理任务”。
- 记录原责任人、代理人、转移时间及代理关系。
- 转移失败的任务进入异常处理队列。

#### 选择“否”

- 不转移委托人的存量待办，存量待办继续由委托人处理。
- 仅代理生效后新产生且符合代理范围的待办。
- 系统记录本次代理配置为“不代理已有待办”。

代理生效后不允许修改该字段。如需改变，应提前结束当前代理并重新创建。

### 5.4 代理范围

- 默认选择“全部”，包含“需求指派”和“用工需求审批”。
- 选择“指定”时，至少选择“需求指派”或“用工需求审批”中的一项，也可同时选择两项。
- 未选中的业务类型继续由委托人处理。
- 允许两类业务设置不同代理人，采用多条独立代理关系管理。
- 同一委托人、同一时间段、同一代理范围只能存在一个生效中的代理人。

### 5.5 设置校验

- 代理人不能是委托人本人，且必须为系统中的有效用户。
- 代理结束时间必须晚于代理开始时间。
- 代理范围为“指定”时至少选择一项。
- 同一委托人、范围和时间段不能存在多个代理人。
- 不允许循环代理、二次代理或多级代理。
- 代理人可以同时代理多名委托人，也可以同期代理不同类型的工作。
- 不限制代理人的原岗位权限，也不授予新的永久岗位或角色权限。
- 校验失败时说明原因、冲突关系及解决方式。

### 5.6 代理确认与处理资格

- 代理人不需要确认或接受，也不允许拒绝代理。
- 到达开始时间后代理自动生效。
- 任务转给代理人后，代理人获得该具体任务的处理资格。
- 处理资格仅针对代理任务，不扩大其他功能或数据访问范围。
- 不设置敏感审批排除规则，所有用工需求审批均可代理。

### 5.7 自动启用

启用条件：当前时间达到开始时间、关系处于待生效、双方账号有效、不存在范围冲突且未被取消。

系统依次执行：

1. 状态更新为“启用处理中”。
2. 建立代理关系并确定代理任务类型。
3. 根据“代理已有待办”决定是否转移存量任务。
4. 开始分配有效期内的新任务。
5. 通知委托人和代理人，记录启用时间、结果及转移数量。
6. 全部成功后更新为“生效中”。

开始时间等于或早于当前时间时，保存后立即启用；未完成任务是否转移仍按字段配置执行。

### 5.8 需求指派代理

- 范围包含“需求指派”时，新产生的需求指派待办自动分配给代理人。
- 委托人不再收到同一任务的新增操作待办，但仍可查看和处理。
- 页面标记“代理处理”，展示原负责人和实际代理人。
- “代理已有待办”为“是”时，转移委托人名下“待指派”的存量需求；为“否”时不转移。
- 已完成、已取消、已关闭、已失效的需求不转移。
- 记录需求编号、原负责人、实际代理人、指派对象、时间和代理关系编号。

### 5.9 用工需求审批代理

- 范围包含“用工需求审批”时，新审批待办自动分配给代理人。
- 待办标题明确显示“请代理审批用工需求”。
- 页面展示原审批人、实际代理人及代理有效期。
- “代理已有待办”为“是”时，转移尚未处理的存量审批；为“否”时不转移。
- 已审批、已撤回、已取消、已失效的任务不转移。
- 审批记录保存原审批人、实际审批人、动作、意见、时间和代理关系编号，并显示“某某代理某某完成用工需求审批”。

### 5.10 委托人处理规则

- 委托人在代理生效期间仍可处理任务。
- 双方看到一致的任务状态；任意一方完成后，另一方待办同步完成或关闭。
- 提交时重新校验状态，已被处理则阻止重复提交，并提示处理人和时间。
- 系统记录实际完成任务的用户。
- 不保留另一方未提交的编辑内容或草稿。

### 5.11 修改、取消与提前结束

- 待生效时，可修改代理人、开始时间、结束时间、代理已有待办、代理范围和说明。
- 生效中仅可修改结束时间和说明；其他字段变更需结束后重新创建。
- 延长或缩短结束时间后，系统通知双方并记录变更前后值和修改人。
- 新结束时间等于或早于当前时间时，经确认后立即结束。
- 待生效代理可以取消；生效中代理可以提前结束。
- 提前结束前展示未完成任务数量，确认后立即停止并转回未完成任务。

### 5.12 自动结束

主要条件为到达设置的结束时间；也可因委托人提前结束、管理员强制结束或双方账号失效而触发。

系统依次执行：

1. 状态更新为“结束处理中”。
2. 停止向代理人分配新任务，使未完成代理任务的处理资格失效。
3. 将未完成的需求指派和用工需求审批转回委托人。
4. 保持已完成的指派和审批结果不变。
5. 后续新任务恢复分配给委托人。
6. 通知双方并记录实际结束时间、方式及任务转回结果。
7. 全部成功后更新为“已结束”。

结束时，代理人未提交的操作不视为完成，也不保留草稿；提交时代理已结束则阻止提交并提示任务已转回原负责人。

### 5.13 异常处理

- 启用、停止分配、任务转移、通知或日志记录失败时，系统自动重试失败步骤。
- 已成功步骤不重复执行，结束异常时优先停止代理人的处理资格。
- 多次失败后向HR或管理员生成异常待办。
- 支持人工重试，并记录原因、失败任务和重试次数。

## 六、待办与通知

| 场景 | 接收人 | 类型 | 内容 |
|---|---|---|---|
| 代理设置成功 | 委托人、代理人 | 通知 | 双方、范围及有效期 |
| 代理即将开始 | 代理人 | 通知 | 开始时间、结束时间及范围 |
| 代理正式生效 | 委托人、代理人 | 通知 | 生效结果及任务入口 |
| 存量待办转移完成 | 委托人、代理人 | 通知 | 类型及数量 |
| 收到代理任务 | 代理人 | 待办 | 委托人及业务信息 |
| 代理即将结束 | 委托人、代理人 | 通知 | 结束时间及未完成数量 |
| 结束时间修改 | 委托人、代理人 | 通知 | 原值、新值及修改人 |
| 代理结束 | 委托人、代理人 | 通知 | 实际结束时间及任务去向 |
| 启用、转移或结束失败 | HR/管理员 | 异常待办 | 原因、影响范围及处理入口 |

- 需执行动作的事项使用待办，结果告知使用通知。
- 待办数量与未读通知数量分开统计。
- 支持结束前提醒，提醒时间由系统统一配置，例如提前1天。

## 七、页面需求

### 7.1 我设置的代理

展示代理人、范围、是否代理已有待办、开始/结束时间、状态、任务数量，以及查看、修改、取消和提前结束操作。

### 7.2 我代理的工作

展示委托人、范围、是否包含已有待办、有效期、剩余时间、两类待办数量及任务入口。

### 7.3 代理详情

展示关系编号、双方、范围、字段配置、状态、设置和修改记录、任务统计、启用/结束记录、通知记录及任务明细。

### 7.4 管理端

支持查询筛选、代设代理、修改结束时间、取消或提前结束、查看执行结果、处理异常、人工重试及导出日志。

## 八、状态设计

```text
草稿 → 待生效 → 启用处理中 → 生效中 → 结束处理中 → 已结束
          ↓           ↓                       ↓
        已取消      启用异常                结束异常
```

| 状态 | 说明 |
|---|---|
| 草稿 | 尚未保存完整设置 |
| 待生效 | 设置成功，尚未到开始时间 |
| 启用处理中 | 正在建立关系并处理存量待办 |
| 启用异常 | 启用或存量任务转移异常 |
| 生效中 | 代理人正在承接工作 |
| 已取消 | 生效前被取消 |
| 结束处理中 | 正在停止分配并转回任务 |
| 结束异常 | 部分结束动作失败 |
| 已结束 | 代理停止且结束动作完成 |

## 九、数据与审计

记录代理关系创建与修改、双方用户、有效期、范围、“代理已有待办”值、实际启停时间、存量识别和转移数量、期间新增任务、双方完成数量、结束转回数量、实际处理人、异常及管理员人工操作。

## 十、核心业务规则

1. 功能不包含请假提交、审批或休假单联动。
2. 代理由开始时间自动启用、结束时间自动停用。
3. “代理已有待办”默认为“是”，存量处理完全按该值执行。
4. 代理范围默认“全部”，也可“指定”需求指派、用工需求审批。
5. 两类业务可以设置不同代理人。
6. 代理人不需要确认且不允许拒绝。
7. 无敏感审批排除，不限制原权限，也不授予永久权限。
8. 委托人在代理期间仍可处理，系统阻止重复提交。
9. 代理人可以同时代理多名委托人。
10. 结束时不保留未提交内容，未完成任务转回委托人，已完成结果保持有效。
11. 支持结束前提醒，所有操作完整留痕。

## 十一、MVP范围

- 新增、查看、修改、取消和提前结束工作代理。
- 设置双方、开始/结束时间、“代理已有待办”和代理范围。
- 两类范围设置不同代理人。
- 定时自动启用、按配置转移存量、新任务自动分配。
- 委托人与代理人共同可处理，并进行并发控制。
- 到期自动结束，未完成任务自动转回。
- 生效、变更、结束、提醒和异常通知。
- HR/管理员管理及异常重试入口。
- 完整任务和审计日志。
